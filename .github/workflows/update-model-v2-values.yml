name: Update Modelo Cronograma V2 Values

# Desativado para execucao automatica.
# A base deve ser processada e validada antes de ser gravada diretamente no repositorio.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Informar modo seguro de atualizacao
        run: |
          echo "A atualizacao automatica por URL temporaria foi desativada."
          echo "Os dados da Modelo Cronograma v2 devem ser validados e publicados diretamente no repositorio."
