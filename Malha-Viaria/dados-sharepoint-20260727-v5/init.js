(async()=>{try{const unzip=async b64=>{if(!b64)throw new Error('Base comprimida ausente');const bytes=Uint8Array.from(atob(b64),c=>c.charCodeAt(0));const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));return JSON.parse(await new Response(stream).text())};const base=await unzip(window.__BB64||'');const detail=await unzip(window.__DB64||'');globalThis.MALHA=Array.isArray(base.malha)?base.malha:[];globalThis.QTY=Array.isArray(base.qty)?base.qty:[];globalThis.DETAIL=Array.isArray(detail)?detail:[];if(!MALHA.length)throw new Error('Malha vazia');if(!DETAIL.length)throw new Error('Detalhamento vazio');delete window.__BB64;delete window.__DB64;const s=document.createElement('script');s.src='./original-app-v3.js?v=20260727-101727';s.onload=()=>{const contrato=document.getElementById('contract');if(contrato&&[...contrato.options].some(o=>o.value==='Todos')){contrato.value='Todos';contrato.dispatchEvent(new Event('change'))}console.info('Malha carregada',{malha:MALHA.length,qty:QTY.length,detail:DETAIL.length})};s.onerror=()=>{throw new Error('Falha ao carregar aplicação')};document.body.appendChild(s)}catch(err){console.error('Erro ao inicializar Malha Viária:',err);const list=document.getElementById('list');if(list)list.innerHTML='<div class="empty">Não foi possível carregar os dados atualizados.</div>'}})();