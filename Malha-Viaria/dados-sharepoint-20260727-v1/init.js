(async()=>{try{const b64=window.__PAYLOAD||'';if(!b64)throw new Error('Payload da base não encontrado');const binary=atob(b64);const bytes=Uint8Array.from(binary,c=>c.charCodeAt(0));const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));const payload=JSON.parse(await new Response(stream).text());if(!payload||!payload.base||!payload.q||!payload.db64)throw new Error('Estrutura do payload inválida');(0,eval)(payload.base);(0,eval)(payload.q);const dbytes=Uint8Array.from(atob(payload.db64),c=>c.charCodeAt(0));const dstream=new Blob([dbytes]).stream().pipeThrough(new DecompressionStream('gzip'));const D=JSON.parse(await new Response(dstream).text());globalThis.MALHA=M.map(x=>({contrato:C[x[0]],rodovia:R[x[1]],kmi:x[2],kmf:x[3]}));globalThis.QTY=(window.__Q||[]).map(x=>({contrato:C[x[0]],periodo:P[x[1]],servico:S[x[2]],quantidade:x[3],rodovias:(x[4]||[]).map(i=>R[i])}));globalThis.DETAIL=D.map(x=>({contrato:C[x[0]],periodo:P[x[1]],rodovia:R[x[2]],servico:S[x[3]],quantidade:x[4],observacao:x[5]||'',kmi:x[6],kmf:x[7],tipo:x[8]?'intervalo':'ponto'}));if(MALHA.length!==165)throw new Error('Malha divergente: '+MALHA.length+' registros; esperado: 165');if(QTY.length!==2024)throw new Error('Quantidades divergentes: '+QTY.length+' registros; esperado: 2024');if(DETAIL.length!==4006)throw new Error('Detalhes divergentes: '+DETAIL.length+' registros; esperado: 4006');delete window.__PAYLOAD;const script=document.createElement('script');script.src='./original-app-v3.js?v=20260727-2';script.onerror=()=>{const list=document.getElementById('list');if(list)list.innerHTML='<div class="empty"><strong>Não foi possível carregar a aplicação.</strong><br>Diagnóstico: arquivo original-app-v3.js indisponível.</div>'};document.body.appendChild(script)}catch(err){console.error('Diagnóstico da base:',err);const list=document.getElementById('list');if(list)list.innerHTML='<div class="empty"><strong>Não foi possível carregar a base atualizada.</strong><br>Diagnóstico: '+String(err.message||err).replace(/[<>]/g,'')+'</div>'}})();